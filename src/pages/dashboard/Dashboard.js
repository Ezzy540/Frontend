import React from "react";

const Dashboard = () => {
  return (
    <div>
      <div>
        dashboard (you have logged in sucessfully if automaticallly redirected
        here (/dashboard) after login)
      </div>
      <div>backend and frontend are Connected</div>
    </div>
  );
};

export default Dashboard;
